# VibeCheck

**사람이 Claude Code CLI로 직접 바이브 코딩할 때 얼마나 효율적으로 만드는지 측정하는 프로그램.** 특히 **"프롬프트만 던졌을 때"보다 "참고 문서/CLAUDE.md/스킬/hook 같은 하네스 엔지니어링 기법을 실제로 활용했을 때" 결과(품질/효율)가 얼마나 더 잘 나오는가**를 측정하는 데 초점을 둔다 — 문제 하나하나가 이걸 드러내도록 설계된다(`docs/problem-set.md`의 "문서 트랩 설계 원칙"). 고정 문제를 대시보드에서 고르면 격리된 워크스페이스가 열리고, 사람이 VS Code + 대화형 Claude Code CLI로 직접 풀고, "완료"를 누르면 시간/토큰/품질을 자동으로 재서 기록한다.

(초기에는 "AI가 헤드리스로 혼자 푼다"는 정반대 방향으로 시작했다가 방향을 바꿨다 — 그 시절 코드/문서는 참고용으로 남아있다: `src/lib/runner.ts`의 `runBenchmark()`, [docs/cli-spec.md](./docs/cli-spec.md). 지금 대시보드는 이걸 안 쓴다.)

## 문서

세부 스펙은 여기 두지 않는다. 필요할 때 아래 문서를 읽어라.

- [docs/overview.md](./docs/overview.md) — 목적, 전체 플로우
- [docs/manual-mode.md](./docs/manual-mode.md) — **지금 유일하게 쓰는 실행 모드.** 대화형 세션 실행/추적/강제종료 메커니즘, 실측으로 발견한 함정들
- [docs/architecture.md](./docs/architecture.md) — 기술 스택, 디렉터리 구조
- [docs/cli-spec.md](./docs/cli-spec.md) — (참고용, 더 이상 안 씀) 예전 헤드리스 자동 모드의 CLI 실행 플래그/결과 JSON 필드. 실측 기반 CLI 동작 지식은 여전히 유효하니 완전히 무시하지는 마라(예: 트랜스크립트 파일 형식, `--safe-mode` 동작 등은 manual-mode.md에서도 그대로 참조함)
- [docs/evaluation.md](./docs/evaluation.md) — 평가 기준(테스트 통과율 + LLM 채점) — 지금도 그대로 유효
- [docs/problem-set.md](./docs/problem-set.md) — 문제 파일 포맷
- [docs/guides/](./docs/guides/) — **문제별 공략집(저자용, 스포일러 포함).** 각 문제의 함정 정체·정답·히든 테스트 기대값·흔한 실패 패턴. run 기록을 해석하거나 문제를 고칠 때 여기부터 본다. ⚠️ 워크스페이스에 복사하거나 참가자에게 보여주면 그 문제는 못 쓰게 된다
- [docs/roadmap.md](./docs/roadmap.md) — 개발 페이즈, 각 페이즈 담당 서브에이전트/스킬 (페이즈 1~6까지는 자동 모드 기준으로 쓰여졌던 것 — 지금은 다 지난 이력)

## 핵심 원칙

- **측정 대상을 오염시키지 마라 — 단, "참가자가 워크스페이스 안에서 직접 구성한 하네스"는 오염이 아니라 측정 대상 그 자체다.** 이 프로젝트가 측정하려는 게 바로 "CLAUDE.md/스킬/서브에이전트를 실제로 활용했는가"라서(위 소개 문단), `--safe-mode`로 그 기법 자체를 막아버리면 애초에 측정을 할 수가 없다는 모순이 있었다(2026-08-05 결론). 그래서 `--safe-mode`는 빼고, 대신 claude.exe 자식 프로세스의 `USERPROFILE`/`HOME`을 run마다 격리된 빈 "가짜 홈"으로 오버라이드한다 — 이 컴퓨터의 진짜 전역 `~/.claude`(전역 CLAUDE.md/hooks/MCP/skills/agents)는 격리해서 안 새어 들어오게 하면서도, 워크스페이스 루트에 참가자가 직접 쓰는 CLAUDE.md/`.claude/skills`/`.claude/agents`는 CWD 기준 탐색이라 그대로 살아있다. `--tools`/`--allowedTools`는 `Read,Edit,Write,Bash,Agent,Skill`로 열어서 서브에이전트/스킬 호출 자체는 가능하게 하되, Workflow/SendMessage 등 오케스트레이션급 도구는 계속 뺀다. 인증은 그 가짜 홈에 구독 로그인(OAuth) 자격증명만 복사해서 그대로 유지한다(실측 검증: `docs/manual-mode.md`).
- **세션은 항상 빈 채로 연다.** 문제 프롬프트를 CLI 인자로 자동 제출하지 마라 — 그러면 사람이 끼어들 틈도 없이 AI 혼자 다 풀어버릴 수 있다(실측으로 확인된 함정, `docs/manual-mode.md`). 사람이 화면의 문제를 보고 직접 타이핑해야 한다.
- **러너는 멍청하게.** subprocess 실행/추적 → JSON 파싱 → 저장. 그 이상의 영리함(재시도, 자동 보정, 휴리스틱)을 넣지 마라. 측정값을 왜곡한다.
- **실행은 항상 격리한다.** 실행마다 새 워크스페이스(프로젝트 밖, `RUN_WORKSPACES_DIR`). 이전 실행 결과나 문제 원본과 섞이면 그 실행은 버려라.
- **프로세스를 죽일 때는 정확한 PID만.** 이 컴퓨터엔 다른 claude.exe 프로세스(데스크톱 앱, 사용자의 다른 실제 세션)가 항상 떠 있다. 절대 이름만으로 매칭하지 마라 — `--session-id`로 넘긴 runId가 CommandLine에 포함된 걸 확인한 프로세스만 죽여라 (`docs/manual-mode.md`).
- **사용량/시간 한도는 하드컷이다.** 문제별 `maxCostUsd`(화면에는 "가중 토큰"으로 환산해 표시)/`maxDurationMs`(`problems/*.json`) 중 하나라도 넘는 순간 즉시 그 프로세스만 죽이고 "실격"으로만 기록한다. 사람이 직접 하는 세션이라고 예외 없음. 유예·경고·부분 채점 없음 (`docs/manual-mode.md`, `docs/evaluation.md`).
- **효율성과 품질은 다른 축이다.** 시간/토큰과 테스트 통과율/LLM 채점을 하나의 점수로 합치지 마라. 사용자가 명시적으로 요청하기 전까지는 분리해서 보여준다.
- **토큰/시간 수치는 항상 "하네스 추정치"라고 밝혀라.** 대화형 세션은 CLI의 공식 구조화 출력 자체가 없다(`--print` 전용 기능). 세션 트랜스크립트를 폴링해서 집계한 값이며, 교차검증 결과 정확했지만 그래도 "추정치"로 표시한다 — 비용은 애초에 알 수 없다(구독 로그인이라 청구 자체가 안 됨).
- **문제 하나가 먼저다.** 여러 문제를 한번에 지원하려 하지 마라.
- **새 문제는 파일 하나로 끝나야 한다.** `problems/*.json` 추가 외에 러너나 평가 로직을 건드려야 한다면 설계가 잘못된 것이다.
- **파일 수정에 PowerShell을 쓰지 마라 — Edit/Write 도구만 써라.** Windows PowerShell 5.1의 `Get-Content`는 BOM 없는 UTF-8 파일을 **시스템 ANSI(CP949)로 읽는다.** `(Get-Content x.md -Raw) -replace ... | Set-Content x.md`는 한 줄짜리 치환처럼 보이지만 한글 파일을 **되돌릴 수 없게 손상시킨다**(CP949로 매핑 안 되는 바이트가 `?`로 치환되고, 그 상태로 UTF-8로 다시 저장된다). 2026-08-14에 이걸로 `docs/problem-set.md`(약 65KB)를 날려서 재작성해야 했다 — git이 없어서 되돌릴 방법도 없었다. 문자열 치환 하나라도 반드시 Edit 도구로 한다.
- **전담 서브에이전트가 있으면 그걸 써라.** 러너/평가/대시보드/실행 디버깅은 각각 담당 서브에이전트가 있다 (`docs/roadmap.md`). 직접 다 하려 하지 말고 위임한다.

## 현재 상태

**수동 모드로 완전히 전환 완료, 실제 브라우저 대신 API 호출로 전체 흐름(시작→VS Code/터미널 오픈→진행률→완료→평가→조회)까지 직접 검증함.**

- `src/lib/manualRun.ts` — 대화형 세션 실행/추적/하드컷(신규, 이게 지금의 실제 실행 엔진). `src/lib/runner.ts`의 `runBenchmark()`(예전 헤드리스 자동 모드)는 코드에 남아있지만 웹 플로우에서 더 이상 안 쓴다 — 삭제하지 않은 이유는 git이 없어서 되돌릴 수 없기 때문(`docs/manual-mode.md`).
- `src/lib/evaluator.ts`, `src/lib/db.ts`의 테스트/OpenAI 채점/저장 로직은 그대로 재사용됨(모드와 무관하게 동일).
- `prisma/schema.prisma`의 `Run.mode`(`auto`|`manual`)로 두 모드의 기존 데이터가 구분된다.
- 대시보드(`/`, `/runs/[id]`, `src/app/api/runs/**`)가 문제 프롬프트 표시 + "완료" 버튼 + 하네스 추정치 라벨까지 반영해서 수동 모드에 맞게 다시 연결됨.
- 실제로 여러 번 끝까지 돌려봄: (1) 프롬프트 자동 제출 버그 발견(아무도 안 건드렸는데 82초 만에 다 풀림) → 프롬프트 제거로 수정, (2) 실사용 중 "토큰이 0으로만 나온다" 버그 발견 → 원인을 좁혀서(트랜스크립트 자체가 안 생기는 문제, `docs/manual-mode.md`) 환경변수 정리로 수정 → 사용자가 직접 대시보드에서 시작→타이핑→완료까지 해봐서 실제 토큰(13,093)이 정상 집계되고 테스트/LLM 채점까지 정상 저장되는 것 확인.
- 문제 세트는 "프롬프트만으로는 안 되고 하네스 엔지니어링(문서/스킬/hook 활용)이 있어야 결과가 잘 나온다"를 측정 대상으로 전면 재설계함 — 기존 문제 4개(csv-to-json-cli, legacy-user-service-refactor, url-shortener-api, word-frequency-counter)는 전부 지우고, 지금은 `csv-summarizer-staged`(문서 트랩), `inventory-digest-staged`(컨텍스트 관리 트랩), `access-audit-staged`(채팅 전달 규칙 유지), `game-tactics-staged`(누적 불변 규칙 + 웹 게임) 네 개가 있다. **랜덤 문제 선택 기능은 뺐다** — 대시보드에서 사람이 직접 문제를 고른다(`src/app/api/runs/route.ts`가 `problemId`를 필수로 요구).
- **단계형 문제(진짜 게이트 방식) 도입**: `csv-summarizer-staged`를 처음엔 "요구사항 4개 + SPEC.md를 전부 한 번에 노출"하는 방식으로 만들었더니, 실사용에서 "spec대로 자동화 구현해줘" 한 마디로 전부 풀려버리는 문제가 발견됐다 — 사람이 문서를 실제로 활용했는지와 무관하게 Claude가 워크스페이스의 SPEC.md를 스스로 찾아 읽고 한 번에 끝내버릴 수 있었기 때문. 그래서 `Problem.stages`/`hiddenTestsPath`(`src/lib/problems.ts`) 스키마를 새로 만들고, 대시보드에 "이 단계 제출" 버튼(`POST /api/runs/[id]/stage` → `manualRun.ts`의 `submitStage()`)을 추가해서, 각 단계를 실제로 통과해야만 다음 단계의 리소스(데이터+갱신된 RULES.md)와 요구사항 텍스트가 워크스페이스/화면에 풀리는 구조로 바꿨다. "완료" 시점엔 `hiddenTestsPath`로 워크스페이스를 전체 히든 테스트로 덮어써서, 나중 단계를 안 풀고 일찍 끝내는 게 점수상 이득이 안 되게 막는다(`docs/problem-set.md`, `docs/evaluation.md`). 통합 테스트로 실측 검증함: 정석 구현은 4단계 전부 통과 + 최종 히든테스트 6/6 통과, naive(인덱스 기반 파싱) 구현은 1단계는 통과하지만 컬럼 순서가 다른 2단계에서 소급 무효화되어 게이트가 막고, 거기서 조기에 "완료"해도 히든테스트가 나머지 단계를 정확히 실패시키는 것까지 확인됨. **`maxTokens` 예산 재검증(2026-08-05)**: 이 통합 테스트는 정답 자체는 확인했지만 토큰 예산까지는 안 봤었다 — 실제 프로덕션 플래그(가짜 홈 + `--safe-mode` 없음, `Read,Edit,Write,Bash,Agent,Skill`)로 4단계를 실제로 처음부터 끝까지 돌려 토큰을 실측한 결과, 실수 없는 이상적인 실행조차 4단계 완료 시점에 609,042토큰을 써서 당시 `maxTokens: 600000`을 1.5% 초과했다(1→88,700 / 2→216,065 / 3→432,069 / 4→609,042 누적). 즉 하드컷이 라이브 세션에서 완료 직전에 무조건 발동해 실격시켰을 것 — 게이트 재시도나 추가 질문까지 감안하면 더 나쁘다. 재시도 여유를 감안해 `maxTokens`를 900,000으로 올림(`problems/csv-summarizer-staged.json`). 시간 한도(`maxDurationMs: 2400000` = 40분)는 에이전트 턴어라운드만 3.3분이라 여유 충분, 안 건드림.
- **컨텍스트 관리(compact/clear) 트랩 문제 추가**: "토큰을 아끼려면 중간에 `/compact`나 `/clear`를 잘 쓰는 게 중요하다"는 하네스 스킬 자체를 측정하는 `inventory-digest-staged` 문제를 새 스키마 필드 없이(기존 `stages`/`maxTokens` 하드컷만으로) 만들었다. 4단계 중 2단계를 재고 리포트 본작업과 무관한 "급한 곁다리 요청"(후기 400개 중 배송 관련 진짜 불만만 골라내기 — 키워드 매칭으론 못 풀고 실제로 읽고 판단해야 함, 실측: 단순 키워드 매칭은 12개 중 4개만 맞음)으로 만들어서 컨텍스트를 크게 불려놓고, 그 컨텍스트를 정리하지 않고 3·4단계까지 들고 가면 누적 캐시 재독 비용 때문에 `maxTokens`(420,000) 하드컷에 걸릴 가능성이 높아지게 예산을 타이트하게 잡았다. 스크립트로 검증 가능한 부분(4단계 게이트 전부 통과, 키워드 매칭 우회가 2단계에서 정확히 걸러짐, 조기 완료 시 히든테스트가 나머지 단계를 실패시킴)은 실측 확인했지만, "실제 compact/clear 사용 여부에 따라 토큰 예산 안/밖으로 갈리는가"는 라이브 세션 의존적이라 이번엔 검증 못 했다 — 정직하게 추정치로 남겨둠, 실사용 후 `maxTokens` 재조정 필요할 수 있음(`docs/problem-set.md`).
  - 처음엔 3·4단계 세부 규칙(카테고리 코드 매핑, 카테고리별 재고 부족 기준)을 다른 문제들처럼 별도 RULES.md 파일로 워크스페이스에 풀어줬는데, "이것도 너무 친절하다"는 피드백으로 뺐다 — 파일로 주면 Claude가 사람 개입 없이 알아서 찾아 읽고 반영해버려서 측정 목적이 흐려지기 때문(SPEC.md 사례와 같은 이유, 아래 "단계형 문제" 항목 참고). 지금은 `promptAddition` 채팅 텍스트에만 규칙을 문장으로 녹여서 준다 — 데이터 파일은 여전히 워크스페이스에 남고 `promptAddition` 텍스트도 대시보드에 누적 표시되므로 컨텍스트를 지우는 것 자체는 안전하지만, 세부 값을 안 놓치려면 사람이 직접 정리해서 다시 챙겨야 한다.
- **"다시 하기"(재시도) 버튼 추가**: 실격/만족스럽지 않은 run 후에 같은 문제를 새 workspace로 바로 다시 시작할 수 있게 했다(`FinishedSummary`, `src/app/page.tsx`). 이전 run의 claude.exe는 정확한 PID로 죽이지만(`manualRun.ts`의 `killClaudeProcessForRun`, best-effort), VS Code 창 자체는 프로세스로 찾아서 강제 종료하지 않는다 — 이 컴퓨터에서 VS Code는 대개 싱글 인스턴스라 이름/커맨드라인 매칭으로 죽이면 사람의 다른 무관한 VS Code 창까지 같이 죽을 위험이 있기 때문(`docs/manual-mode.md`). 대신 VS Code CLI가 공식 지원하는 `-r`(마지막 활성 창 재사용) 플래그로 새 창 없이 그 자리에서 새 워크스페이스로 바꿔치기한다(`runner.ts`의 `tryOpenVsCode`/`createWorkspace`에 `reuseWindow` 옵션 추가, `POST /api/runs`가 `retryFromRunId`로 트리거).
- **"토큰 하나"에서 "토큰 + 비용" 두 축으로 분리(2026-08-07)**: 실사용 중 "Fable로 하는 거랑 Sonnet으로 하는 거랑 토큰이 똑같이 올라가는데 모델마다 단가가 다르지 않냐"는 지적이 나왔다. 확인 결과 집계가 모델도 토큰 종류도 전혀 구분하지 않고 `input+output+cache_creation+cache_read`를 1:1로 더한 숫자 하나만 쓰고 있었다. 먼저 사실관계: **토큰 개수가 모델마다 다른 게 아니다** — Fable 5/Opus 5/Sonnet 5는 같은 토크나이저 계열이라 같은 텍스트면 토큰 수가 거의 같다. 벌어지는 건 토큰당 단가(정가 기준 Fable 5는 Sonnet 5의 3.33배)와 토큰 종류별 배수(캐시 읽기 0.1배 / 5분 쓰기 1.25배 / 1시간 쓰기 2배)다. 필요한 정보는 트랜스크립트에 전부 있었다(`message.model`, `usage.speed`, `usage.cache_creation`의 TTL별 분리 — 실측상 CLI는 1시간 TTL을 쓰므로 뭉뚱그렸으면 캐시 쓰기를 60% 과소평가). 단가 테이블/환산은 `src/lib/pricing.ts`에 두고, `manualRun.ts`의 집계가 `{ totalTokens, byModel, compactApproxTokens }`를 돌려주도록 바꿨다. **적용 범위는 사용자가 정했다**: 하드컷은 토큰 그대로(문제별 `maxTokens`가 실측 값이라 기준을 바꾸면 전부 재산정 + 과거 run과 비교 단절), 표시는 두 축 병기, 종합 점수의 토큰효율 30%만 비용 정규화(`실제 토큰 × 기준 모델 대비 비용 배율`, 기준 모델 Sonnet 5라 기존 `referenceTokens`를 손댈 필요 없음). 비용은 구독 로그인이라 실청구가 아니라 "API 종량제로 돌렸다면" 환산치임을 화면에 항상 밝힌다. 과거 run 14건에 옛/새 계산식을 같은 파일에 나란히 돌려 **14건 전부 옛 계산식 == 새 계산식 == 모델별 분해 합계**로 일치하는 것 확인함(하드컷 기준 불변 검증). `/compact` 근사치는 모델 정보가 없어 직전 turn 모델에 귀속 + 캐시 읽기 단가로 환산하는데, 이건 추정 위에 얹은 가정이라 `compactApproxTokens`로 따로 노출한다(`docs/manual-mode.md`, `docs/evaluation.md`).
- **`/clear`를 쓰면 토큰 집계가 멈추던 버그 발견 → 수정(중요)**: 사람이 세션 중 `/clear`를 입력하면 Claude Code CLI가 원래 `--session-id`로 지정한 runId와 무관한 새 sessionId로 같은 프로젝트 디렉터리 안에 새 `.jsonl` 파일을 만든다는 걸 실측으로 확인했다(워크스페이스 하나에 서로 다른 sessionId 파일 3개가 쌓여있었음). 기존엔 시작 시점 파일 하나만 계속 폴링해서(`computeTranscriptPath`) `/clear` 이후 사용량이 전혀 안 잡히고 집계가 그 자리에서 멈췄다(실측: 143,616에서 멈췄지만 실제로는 450,831까지 썼음 — 68%가 안 잡히고 있었다). 이건 부정확함을 넘어 `/clear`로 `maxTokens` 하드컷 자체를 우회할 수 있는 버그였다(`CLAUDE.md`의 "토큰/시간 한도는 하드컷이다" 원칙 위배, `inventory-digest-staged`의 컨텍스트 관리 트랩 측정 전제와도 직결됨). 파일 하나가 아니라 **워크스페이스 프로젝트 디렉터리 전체**를 폴링마다 다시 나열해서 합산하도록 고쳤다(`manualRun.ts`의 `sumWorkspaceUsage`). 실제 버그 재현 데이터로 수정 후 값이 정확히 일치하는 것까지 확인함(`docs/manual-mode.md`).
- 대화형 claude.exe는 더 이상 별도 Windows Terminal 창이 아니라 **생성된 워크스페이스의 VS Code 통합 터미널**에서 자동 태스크(`.vscode/tasks.json`, `runOn: folderOpen`)로 뜬다(`src/lib/manualRun.ts`의 `writeVsCodeAutoRunTask`) — 창 하나로 통합하기 위한 변경. 대가로 처음 여는 워크스페이스마다 VS Code 워크스페이스 신뢰 창을 사람이 한 번 클릭해야 하는데, `vibecheck-runs` 상위 폴더를 한 번만 "부모 폴더 전체 신뢰"로 신뢰해두면 이후로는 안 뜬다(`docs/manual-mode.md`).
- **채점자는 화면이 실제로 동작하는지 못 잰다 → 히든 테스트가 진짜 브라우저로 클릭하게 바꿈(2026-08-14)**: 실사용자가 "공학계산기 만들어줘 한 번 프롬프트했는데 괄호 연산도 안 되고 몇몇 기능이 안 되는데 90점"이라고 보고했다. 원인은 채점 버그가 아니라 **측정 사각지대**였다. 참가자 산출물의 `ui.js`가 `querySelectorAll("#pad button[data-insert]")`로 리스너를 걸었는데 괄호·√·log·% 버튼은 전부 `#sciPad` 안이라 **클릭 무반응**이었다(키보드 타이핑만 됐다). 그런데 히든은 `calc.js`의 `evaluate()`만 돌리고 화면은 한 줄도 실행하지 않으므로 **7/7 통과가 맞았고**, 화면은 rubric(소스 읽기)만 봤다. 여기에 채점 프롬프트가 "자동 테스트 전부 통과 = **결과물이** 사양대로 나온 건 확정된 사실"이라고 **산출물 전체를 가리키게** 쓰여 있어서 채점자가 화면까지 통과한 셈 쳐서 4점을 줬다 → 90점. **수정 ①**: 그 안내의 범위를 "**테스트가 실제로 실행한 부분**만 확정, 테스트가 안 닿는 영역(화면/DOM 배선 등)은 직접 확인하라"로 좁혔다(모든 문제에 적용되는 공통 수정). 같은 워크스페이스 재채점 **86.7% → 80.0%**. **수정 ②(핵심)**: 그걸로 부족하다는 걸 실측했다 — rubric에 "리스너 셀렉터가 모든 버튼을 덮는지 대조하라, 셀렉터 밖 버튼이 있으면 낮은 점수"라고 콕 집어 써도 채점자는 **여전히 못 잡았고** 오히려 "괄호/루트/로그 화면 입력 가능"에 5점을 줬다(전부 죽어있는데). **소스 읽기로는 화면 동작을 못 잰다는 게 2회 실측으로 확정**돼서, `calculator-web-staged`의 히든에 **설치된 Chrome(없으면 Edge)을 헤드리스로 띄워 버튼을 진짜 클릭하고 `#result`를 읽는** 테스트를 넣었다(`tests/chrome.js`, **의존성 0** — Node 24 내장 `fetch`/`WebSocket`으로 CDP 직접 제어, 워크스페이스는 채점기가 자체 서버로 서빙해서 참가자의 `server.js` 수정과 무관하게 만듦). 성립 조건은 **"어디를 눌러 어디를 읽는가"를 규약으로 못박는 것**이다 — 결과는 `id="result"`, 버튼은 `data-key`(괄호는 3단계, `%`는 6단계에 맞춰 도착). 마크업/프레임워크/디자인은 그대로 자유이고, `evaluate()` 시그니처를 못박은 것과 같은 종류의 계약이다. **검증(17개 체크)**: 정석(계산+화면) 8/8 통과 / 스텁은 7/8 실패 / **실사고 재현 — `calc.js`는 정석 그대로 두고 셀렉터만 한쪽 컨테이너로 좁힌 페이지가 화면 테스트만 정확히 실패** / 계산이 깨진 변형들은 해당 계산 test와 화면 test에서 같이 걸림(사람이 화면에서 보는 값이 그거라 맞다). ⚠️ **채점 머신에 Chrome/Edge가 있어야 한다** — 없으면 조용히 건너뛰지 않고 명시적으로 실패시킨다(건너뛰면 깨진 화면이 만점으로 통과한다). ⚠️ **부수 관찰**: 이 run은 프롬프트 한 번으로 끝나서 이 문제가 재려던 "스펙 누적" 축이 아예 작동하지 않았다 — "공학계산기"라는 말 한마디에 괄호·√·log가 전부 선반영된다. 브리프 §0이 예고한 한계가 실제로 나타난 것(`docs/evaluation.md`, `docs/guides/calculator-web-staged.md`).
- **`calculator-web-staged` 추가 — 게이트를 일부러 없앤 워밍업 문제(2026-08-14)**: `docs/example/calculator-web-staged.md` 브리프대로 스캐폴딩했다. 기존 문제가 전부 "함정 있는 고난도 단계형"이라 **처음 온 사람이 몸 풀 문제가 없었고**, 쉬운 과제에서의 시간·토큰 기준선도 없었다. 이 문제가 그 자리를 채우면서 동시에 **다른 문제들과 정반대의 축**을 잰다: 기존 문제는 누적 게이트가 "뒤 단계가 앞 단계를 깼는가"를 매 단계 잡아주는데, **잡아주지 않는 것 자체가 측정 축**인 경우가 있다 — 바이브 코딩의 가장 흔한 실패 모드가 "방금 요청받은 것만 고치고 앞 기능을 회귀시키는 것"인데 누적 게이트가 그걸 대신 잡아주면 측정할 기회가 사라진다. 그래서 **스키마 변경 없이 게이트를 무력화**했다: 모든 단계의 `gateTestCommand`가 `node -e ""`(워크스페이스 상태와 무관하게 무조건 통과)이고 `unlockPath`는 전부 `null`이며, 요구사항은 손님이 슬쩍 물어보는 확인 질문(`promptAddition`)으로만 도착하고, **완료 시점 히든 한 번**이 1~6단계 요구가 동시에 살아있는지 검사한다. **검증 완료(16개 체크, `scripts/verify-calculator-web-staged.mjs`)**: 빈 디렉터리로도 6단계 전부 통과(게이트가 정말 무력함) / 정석 구현 히든 7/7 / 한 군데씩만 깬 구현 4종이 **정확히 그 test만** 실패(우선순위 되돌림→3단계, 소수점 정리 누락→5단계, ÷0 가드 제거→4단계, 자연로그→6단계) / 스타터에 2~6단계 함정 미노출 / 실제 VS Code 창에서 자동 태스크 2개(claude 세션 + 서버)가 같이 뜨고 서버가 Hello World 응답. **스키마 두 필드 추가**(둘 다 optional·범용): `listOrder`(대시보드 목록 정렬 — 첫 항목이 기본 선택값이라 워밍업을 맨 위에 두려고), `autoStartCommand`(워크스페이스를 열 때 같이 띄울 명령 — 웹 문제에서 "열자마자 페이지가 떠 있는" 상태를 만든다, 포커스는 claude 터미널이 가져간다). **첫 실사용 직후 예산/시간 3배 상향(같은 날)**: 처음 어림잡은 $1.0 / 기준선 400,000 / 적정 5분→하드컷 15분으로 실제로 풀어보니 **토큰도 시간도 모자랐다**. 워밍업 문제는 기준선 역할을 하려면 정상 시도가 전부 완주해야 해서 실격을 유도하면 안 되므로 $3.0 / 기준선 1,200,000 / 백스톱 6,000,000 / 적정 10분→하드컷 20분으로 올렸다. **교훈: "쉬운 과제"는 코드 난이도지 턴 수가 아니다** — 요구가 6번에 나눠 도착하고 매번 화면까지 손보므로 턴이 계속 쌓인다(턴당 약 42,300 가중 토큰). 새 문제 예산을 코드 난이도로 어림하지 마라. ⚠️ 상향값도 한 번의 실사용에 맞춘 것이라 run이 쌓이면 실제 분포로 다시 조여야 한다. **"이 문제로는 하네스 엔지니어링이 안 갈린다"는 결함이 아니라 의도다**(브리프 §0) — 변별은 회귀 여부와 엣지케이스에서만 미세하게 난다(`docs/problem-set.md`, `docs/guides/calculator-web-staged.md`).
- **세션을 나누면 도구 세트가 통째로 달라지고 추적이 끊기던 버그 → PATH 심으로 수정(2026-08-14)**: 실사용자가 `handover-relay-staged`를 풀며 세션을 분리하다 발견했다 — `/context`의 tools가 자동 시작 세션에선 9.4k인데 수동으로 연 터미널에선 30k. 원인은 가짜 홈 누출과 **같은 뿌리의 두 번째 누출**이었다: 가짜 홈/텔레메트리는 `terminal.integrated.env.windows`로 새 터미널까지 덮어놨지만, 도구 제한은 env가 아니라 **CLI 플래그**(`--tools/--allowedTools/--strict-mcp-config`)로 걸려 있어서 `tasks.json`이 띄우는 자동 태스크 세션에만 적용됐다. 실측: 같은 빈 프롬프트의 프리픽스가 제한 세트 **16,326토큰** vs 기본 세트 **29,504토큰**(1.8배). **세션을 나누는 건 우리가 측정하려는 숙련 행동 자체**라(`handover-relay-staged`는 그걸 재려고 만든 문제다), 나눈 사람만 매 턴 13k를 더 이고 다니면 효율 축 비교가 성립하지 않는다. **수정**: 가짜 홈 밑(워크스페이스 밖)에 `claude.cmd`/`claude`(Git Bash용) 심을 만들어 자동 태스크와 **완전히 같은 플래그**로 진짜 exe를 대신 실행하게 하고, `PATH`를 `<shimDir>;${env:PATH}`로 심어 이 폴더의 모든 터미널이 심을 먼저 잡게 했다(`manualRun.ts`의 `createClaudeShimDir`/`workspaceSessionEnv`). `--session-id`는 자동 태스크가 이미 점유하고 있어 충돌하므로 심은 `--name vibecheck-<runId>`로 runId만 CommandLine에 남긴다. ⚠️ **여기서 한 번 깨뜨렸다**: PATH를 tasks.json의 `options.env`에도 넣었더니 거기서는 `${env:PATH}`가 치환되지 않아 PATH가 통째로 깨졌고, 태스크가 `claude.exe`를 못 찾아 `Path to shell executable "...\<workspace>\claude.exe" does not exist`로 **세션이 아예 안 떴다**(실사용 즉시 발견). 치환 동작이 두 자리에서 다르다 — 터미널 설정에서만 된다. 지금은 태스크 env에 PATH를 안 주고 `command`를 진짜 exe **절대경로**로 박아 PATH 의존을 없앴다. **검증**: 심 세션의 프리픽스가 제한 세트와 캐시 히트로 정확히 일치(cache_read 16,326), CommandLine 매칭 동작, `${env:PATH}`가 실제 VS Code 터미널에서 치환되는 것까지 실제 창에서 확인, 생성 파일 정합성 20개 체크(`scripts/verify-terminal-shim.ts`), **그리고 실제 VS Code 창을 열어 자동 태스크가 정말 claude.exe를 띄우는 것까지 확인**(`scripts/verify-autotask-launch.ts` — 파일 검사만으로는 위 PATH 사고를 못 잡았기 때문에 추가했다). **같이 고친 것**: `pollOnce`가 "추적하던 PID 사망 = 세션 종료"로 보고 그 자리에서 얼려버려, 세션 분리의 공백(1번 종료 → 새 터미널 → 2번 시작)에서 run이 끝난 것으로 확정되고 **이후 세션 사용량이 진행률에도 하드컷에도 안 잡히던** 누출(`/clear` 버그와 같은 종류) — 이제 같은 run의 다른 claude.exe로 추적을 넘기고, 없으면 3분 유예 후 **사라진 시점의 스냅샷으로** 얼린다(유예 시간이 소요 시간/시간 감점에 섞이지 않게). 강제 종료도 이 run에 속한 프로세스 **전부**를 죽인다(하나만 죽이면 나머지 세션이 하드컷 후에도 예산을 계속 태운다). ⚠️ **미검증**: 라이브 대화형 세션에서 사람이 실제로 새 터미널을 열어 `/context`가 9.4k로 나오는지는 사람이 타이핑을 해야 확인된다 — 다음 실사용에서 확인 필요(`docs/manual-mode.md`).
- **`access-audit-staged` 문제 추가 + "스킬은 토큰을 아껴준다"가 실측으로 반증됨(2026-08-10)**: "Skill 사용법 자체를 알려주는 문제"를 요청받아, 규칙을 **오직 채팅으로만** 주고 4단계 내내 그 규칙이 필요하게 하고 로그를 크게(누적 299KB) 준 문제를 만들었다. 전제는 "원본을 컨텍스트에 올리면 비싸니 스킬/스크립트로 오프로딩해야 통과한다"였다. **실사용자가 "스킬 써도 토큰 차이가 없다"고 보고 → 늘리기 전에 전제를 실측했고, 전제가 틀렸다.** ① 비용은 바이트가 아니라 **턴**에서 나온다 — 저장된 run 8건(247턴) 분해 결과 **턴당 $0.0423(42,300 가중토큰)**, 비용 구성은 컨텍스트 재독 42%/출력 27%/신규 적재 31%. SKILL.md 2,000토큰을 30턴 들고 다녀도 0.4턴 어치(예산의 1.2%)라 캐싱 앞에서 무의미하다. ② 판단 반복 작업으로 4-arm A/B를 헤드리스로 돌린 결과 **스킬 쪽이 한 세션에서 20%, 새 세션에서도 12% 더 비쌌다** — 스킬을 찾고 SKILL.md를 읽는 도구 호출로 **배치당 +2.3턴**이 붙는데, 지시문을 프롬프트에 그냥 붙여넣는 건 턴이 안 늘기 때문. **손익분기: 매번 재입력해야 하는 지시문이 약 16,450토큰(56KB) 이상**이어야 스킬이 본전이고, 한 세션을 유지하면 재입력 자체가 없어 이길 방법이 없다. ③ 부수 발견: **`/clear`는 54% 더 비싸다**(새 세션마다 시스템 프롬프트/도구 정의를 캐시 쓰기 가격으로 재적재) — "컨텍스트를 비우면 이득"이라는 통념도 이 규모에선 안 맞는다. **스킬이 실제로 이긴 곳은 비용이 아니라 정확도였다**(A/B에서 스킬 arm만 18/18 만점). 그래서 스킬은 토큰 축이 아니라 **품질 축**에서 측정해야 한다. 이 문제의 예산 근거($1.50)는 무너져서 다른 문제와 같은 $2.00으로 되돌렸고, 무의미해진 rubric 항목("로그를 대화에 안 끌어들이는 구조인가")도 교체했다. **현재 이 문제의 실제 측정 대상은 스킬이 아니라 "채팅으로만 전달된 비관습 규칙을 정확히 반영하고 단계가 쌓여도 앞 규칙을 안 깨뜨리는가"**이며 그 부분은 검증됐다(정석 4단계+히든테스트 통과, naive는 1단계에서 탈락, 조기 완료는 히든테스트가 차단). **미해결**: 산출물이 코드라 규칙이 `audit.js`에 박히면 코드가 규칙 저장소가 돼서 품질 축에서도 스킬이 개입할 여지가 없다 — 제대로 측정하려면 코드에 안 박히는 판단 작업이 단계에 섞여야 한다(`docs/problem-set.md`).
- **집계를 CLI 텔레메트리로 바꾸고, 토큰/비용을 "가중 토큰" 한 축으로 통합(2026-08-10)**: "이 프로그램이 재는 수치가 Claude Max 구독의 사용 한도 소모와 같았으면 좋겠다"는 요구에서 출발했다. 공식 문서 확인 결과 구독 한도는 **토큰 1:1 합이 아니라 모델·토큰 종류로 가중된 값**으로 소모된다(캐시 읽기는 캐시 요율로 잡히고, 모델과 effort가 사용량에 영향, 그 위에 Opus 전용 캡이 별도로 존재). 즉 한도 소모에 비례하는 건 토큰 수가 아니라 비용 축이다. 저장된 run 17건 재계산으로 실제 괴리를 확인함: **raw 토큰 1M당 비용이 $0.62~$1.58로 2.5배까지 벌어졌고, 토큰이 30% 적은 run이 한도는 49% 더 쓴 순위 역전**까지 있었다. 두 가지를 바꿨다. ① **집계 소스 교체** — 트랜스크립트 폴링은 `.jsonl`에 usage가 안 남는 호출(대화 제목 생성용 Haiku 배경 호출, `away_summary`, `/compact` 실제 비용)을 원천적으로 못 잡는다. Claude Code의 OpenTelemetry 출력을 대시보드가 직접 받도록 바꿨다(`src/lib/telemetry.ts` + `src/app/api/otel/v1/metrics/route.ts`, `manualRun.ts`가 `OTEL_*` env를 tasks.json/settings.json 양쪽에 주입). 검증: 헤드리스 세션에서 OTel 합계가 CLI 공식 `modelUsage`와 **모델별 토큰 0 차이, 비용 0 차이**로 일치했고 놓치던 haiku 호출(603토큰)도 잡혔다. 텔레메트리가 안 붙으면 기존 트랜스크립트 집계로 폴백하되 **총 토큰이 큰 쪽**을 쓴다(작은 쪽을 쓰면 하드컷이 느슨해진다 — `/clear` 버그와 같은 종류). 출처는 `Run.harnessEstimateSource`에 저장하고 화면에 밝힌다. 두 함정 주의: OTel 메트릭은 **delta temporality라 전부 더해야** 하고(마지막 것만 쓰면 3배 과소집계), run 귀속은 `OTEL_RESOURCE_ATTRIBUTES`의 커스텀 속성으로 해서 `/clear`에도 안 깨진다. ② **토큰 축과 비용 축을 "가중 토큰" 하나로 통합** — 제한은 비용(`maxCostUsd`)으로 걸고, 표시는 그 비용에 가중치가 반영된 토큰으로 한다: **가중 토큰 = 비용 × 100만**(100만 = $1, `src/lib/pricing.ts`). 눈금을 실측 중앙값($1.001/1M raw)에 맞춰서 **전형적인 run이면 가중 토큰 ≈ raw 토큰**이 되게 잡았다 — 덕분에 하드컷 $2.00이 정확히 2,000,000 가중 토큰이라 기존 예산 숫자와 감각이 그대로 유지되면서도 가중치는 온전히 살아있다(실측 가중/실제 비율 0.62~1.58배). `maxTokens`는 폭주 백스톱으로 강등(텔레메트리가 끊긴 채 단가 미등록 모델이 섞인 경우에만 발동), `referenceTokens`는 `referenceWeightedTokens`로 이름을 바꿔 같은 축에 올렸다 — 이제 하드컷·점수·화면이 전부 같은 숫자 하나를 쓴다. 저장된 manual run 30건에 새 코드를 그대로 돌려 **하드컷 판정이 뒤집힌 run 0건** 확인. 비용 데이터가 없는 옛 run은 raw 토큰을 근사로 쓰고 화면에 "근사"라고 밝힌다. **의도된 부작용: 같은 문제도 Opus로 풀면 비용 예산을 더 빨리 소모한다** — 실제로 구독 한도를 그만큼 더 쓰기 때문이다. 한계: "내 주간 한도의 몇 %"라는 진짜 수치는 `/usage`가 읽는 서버 엔드포인트에만 있고 공개 API가 없어서 못 가져온다 — 만들 수 있는 건 비례하는 양이지 비율이 아니다. **아직 라이브 대화형 세션에서는 검증 못 했다**(헤드리스 + 생성된 설정 파일까지만 확인) — 사람이 타이핑을 해야 토큰이 발생하기 때문. 다음 실사용 run에서 확인 필요(`docs/manual-mode.md`, `docs/evaluation.md`).

- **`game-tactics-staged` 추가 — 문서화 효과를 "누적 불변 규칙"으로 재는 웹 게임 문제(2026-08-10)**: "CLAUDE.md/docs에 플랜을 적었을 때 효율/정확도가 좋아지는 걸 체감할 수 있는 문제를, 웹페이지에서 게임 만드는 방식으로"라는 요청. 격자 턴제 전술 게임을 4단계로 만들되 규칙은 **채팅으로만** 준다. 문서가 이기는 지점은 "규칙을 적어뒀나"가 아니라 **앞 단계 규칙이 뒤 단계 작업에서 깨지는가**라서, 4단계 궁극기가 `1단계 데미지 공식 → 2단계 지형 보정(대상별 개별 적용) → ×1.5 버림`을 전부 다시 타게 하고 3단계 독은 그 규칙들의 예외(지형 무관/게이지 안 채움/hp 1 하한)로 두었다 — 하나만 빠뜨려도 게이트가 앞 단계 테스트부터 되살려 실패시키고, 그 재시도 루프가 곧 토큰 비용이다. 로직은 `game.js` 순수 함수로 분리시켜 `node --test`로 결정론적 채점하고, 화면(`index.html` + 의존성 없는 `server.js`)은 rubric으로만 본다. **설계 전제였던 "앞 단계 테스트 불변성"은 생성기가 모든 시나리오를 자기 레벨과 4레벨에서 각각 돌려 결과 동일성을 검증한 뒤에만 emit하도록 강제했다**(처음엔 피격당 게이지 +20이라 1단계 시나리오에서 궁극기가 터져 검증 실패 → +10으로 수정). 검증 완료: 정석 4단계+히든 통과 / 관습적 구현(빼기 데미지·아군 우선·체비셰프 사거리)은 1단계 탈락 / 조기 완료는 히든이 차단 / 4단계에서 지형 보정을 건너뛴 회귀는 게이트가 검출. **미검증(정직하게 남김)**: "문서를 쓴 사람이 실제로 토큰을 덜 쓰는가"는 안 쟀다 — 앞선 A/B에서 확인했듯 한 세션을 유지하면 규칙이 컨텍스트에 남아 문서가 개입할 여지가 없고, 이 문제는 분량이 커서 compact가 걸릴 것이라는 가정에 기대고 있다. 예산(`maxCostUsd: 3.0`, 120분)도 실측이 아니라 턴당 $0.0423 기준 어림이라 첫 실사용 후 재조정 필요(`docs/problem-set.md`).

- **`report-xlsx-staged` 추가 — "외부 공식 스킬 생태계를 스스로 찾아 쓰는가"를 사후 관찰로 재는 문제(2026-08-11)**: `docs/example/report-xlsx-skill-staged.md` 브리프대로 스캐폴딩했다. 지금까지 스킬 관련 문제가 "우리가 준 규칙을 하네스로 박아두는가"를 쟀다면, 이건 **우리가 스킬의 존재조차 힌트로 주지 않은 상태에서 참가자가 `anthropics/skills`의 `xlsx` 스킬을 스스로 떠올려 설치해 쓰는가**를 잰다. A/B를 config로 강제하지 않고 문제 파일 하나만 두며, 갈리는 행동은 나중에 트랜스크립트/워크스페이스 diff로 사후 분류한다. 변별축은 문서 독해가 아니라 **라이브러리 정확성** — 매출 csv를 xlsx 리포트로 만드는 4단계이고, 핵심 함정은 2단계의 "요약 시트 합계가 살아있는 수식이면서 **값도 파일에 들어있어야** 한다"이다(채팅에는 "저희는 엑셀 말고 사내 뷰어로 열어봐서 합계가 빈칸이면 안 돼요"라는 업무 언어로만 적었다). exceljs는 `{formula, result}`를, openpyxl은 LibreOffice 재계산을 요구하는데 **공식 SKILL.md가 정확히 이 둘을 맨 앞에서 경고한다** — 스킬을 쓴 쪽은 알고 시작하고 안 쓴 쪽은 "실행→빈칸→수정" 루프를 돈다는 게 가설이다. **검증 완료**: 정답이 두 런타임 모두 20/20 통과(openpyxl은 재계산 전엔 캐시값 테스트 하나만 실패 — 의도한 지점에서 정확히 걸림), 함정을 하나씩만 밟은 구현 10종이 전부 의도한 단계에서만 탈락, 단계 오프셋/조기 완료 차단, 참가자 노출 텍스트 키워드 검사 0건. **인프라도 직접 확인함**(브리프가 최우선으로 지정): 가짜 홈 격리 안에서 `git clone anthropics/skills` · `npm install exceljs` · `import openpyxl, pandas` · LibreOffice 재계산이 전부 성공한다. **의도적 이탈 하나** — 브리프가 지정한 파일명 `report-xlsx-skill-staged`를 `report-xlsx-staged`로 바꿨다(워크스페이스 폴더명이 `${problem.id}_...`라 참가자 VS Code 제목에 "skill"이 그대로 노출된다). **미검증(정직하게 남김)**: 브리프 8.4의 헤드리스 A/B — "스킬 쓴 쪽이 실제로 더 낫다"는 아직 가설이다. 이 저장소의 앞선 실측("스킬은 토큰 절약 수단이 아니다", 조회에 배치당 +2.3턴)과 방향이 반대라 실제로 재봐야 안다. 예산(`maxCostUsd: 2.5`, 40분)도 어림이고, 직접 구현 경로도 완주 가능해야 비교 데이터가 모이므로 일부러 여유 있게 잡았다(`docs/problem-set.md`).

- **`handover-relay-staged` 추가 → 실사용 실측으로 전제가 반증되어 v2로 전면 개정(2026-08-13)**: `inventory-digest-staged`와 같은 명제("무관한 업무를 한 세션에 이고 가면 앞 업무의 큰 데이터가 매 턴 캐시 재독으로 나가 비싸다")를 재되, 그 문제가 실패한 원인(**표로 주면 스크립트로 접혀서 원본이 대화창에 안 올라온다**)을 고쳐 **데이터를 산문으로** 줬다. 큰 산문에서 확정값을 조회하게 하면 통째로 읽는 게 자연스러우면서도 정답은 기계적으로 확정되고, **naive와 숙련이 똑같이 정답을 내고 토큰만 갈린다.** ⚠️ **그런데 v1을 실제로 두 번 돌린 결과가 v1의 전제를 반증했다** — Haiku run(cd85075a)은 문서를 통째로 상주시켜 turn당 cache_read가 16,669→83,848로 단조 증가하고 **raw 토큰의 96%가 상주 재독**이었는데도 **$1.0 예산의 42%**밖에 안 썼고(Haiku cache_read 단가가 $0.10/1M), 탈락 사유도 토큰이 아니라 **품질**(문서와 무관한 단계의 표기 정규화 실패)이었다. Sonnet run(de559ff8)은 1단계에서 멈춰 "끊어서 아낄 구간"이 0이었다. **여기서 나온 일반 결론 둘**: ① cache_read가 input보다 10배 싸서 **큰 자료를 이고 가는 벌점은 싼 모델에선 거의 안 느껴진다**(Sonnet은 재독이 Haiku의 3배라 여기서만 체감된다), ② **적재 비용(cache_write)은 두 경로가 똑같이 낸다** — 끊어서 아끼는 건 "적재 후 이고 간 턴"의 cache_read뿐이라 차이를 키우려면 **블록 수 × 이후 무관 턴 수**를 키워야 한다. 그래서 v2는 **무거운 산문 블록 3개**(A 인프라 `handover.md` 35,722토큰 / B 결제 `tickets.md` 38,021 / C 데이터 `access-requests.md` 40,125, 누적 피크 113,868로 auto-compact 안전선 아래)를 서로 다른 직무에 적재하고 각 블록 뒤에 무관 업무를 뒀다. **함정은 개수가 아니라 격차로 고른다** — 쌓으면 Haiku만이 아니라 Sonnet도 같이 깎여서(4개면 Sonnet 전부통과 81%) 효율 축 측정 자체가 무너지므로, 격차 큰 **3가족만**(override 체인 / 다변형 정규화 병합 / 다단 조건+경계값) 쓰고 4·6단계는 무함정 집계 단계로 둬서 턴만 늘렸다. **두 실패를 다른 축으로 분리한다: Haiku는 품질에서, 단일 세션은 토큰에서 탈락.** **검증 완료(74개 체크)**: 정석 solver를 상수 복사가 아니라 워크스페이스 파일에서 직접 파싱하도록 짜서 "데이터가 정답을 유일하게 결정하는가"까지 확인했고 — 이 덕분에 **문서 자체의 모호함을 실제로 잡았다**(승인자 이름 "박팀장"이 직급 "팀장"과 문자열 충돌해서 정석 구현조차 5·6단계에서 틀림 → 직급을 `팀장급`/`본부장급`/`임원`으로 분리) — 정석 1→7단계+히든 전부 통과 / 오답 10종이 의도한 단계에서만 실패 / 단계 오프셋 / 조기 완료 차단 / 말끔함 0건. ⚠️ **미검증(합격선이 여기 있다)**: v2 개정안이 합격선으로 지정한 **Sonnet 2-arm 토큰 A/B(≥30% 차이)**, **Haiku ×5 전탈락 / Sonnet ×5 전통과**, **auto-compact 무발생**은 전부 라이브 모델 실행이 필요해서 안 돌렸다. 예산(`maxCostUsd: 3.0`, 35→60분)도 **안전형 잠정값**이다 — 개정안의 이상형은 "단일 세션 실격/끊으면 통과"지만 실측 없이 조이면 정상 시도까지 전원 실격시킨다(`csv-summarizer`·`inventory-digest`에서 두 번 겪음). 기준 모델은 Sonnet 5이고 **이 문제는 사실상 Sonnet 전용**이다(`inventory-digest`가 Haiku 전용인 것의 정반대). (`docs/problem-set.md`, `docs/guides/handover-relay-staged.md`)

- **채점기가 "손도 안 댄 입력 파일"을 읽고 참가자를 감점하던 버그 수정(2026-08-13)**: 위 v1 run 두 건을 분석하다 발견했다. `evaluator.ts`의 `collectWorkspaceSource`가 **문제가 제공한 읽기전용 입력 파일을 채점 프롬프트에서 안 뺐고**, 그래서 일부러 뒤죽박죽으로 만든 `handover.md` 앞부분 12,000자가 두 run 모두 채점기에 실렸다. 저장된 감점 사유가 그대로 남아 있다 — "handover.md 자체가 여러 직무 문구를 뒤섞어 서술하고 있어…"(직무 분리 **2점**), "handover.md는 매우 장황하고 반복이 많아…"(가독성 **3점**). **입력의 성격이 참가자 점수가 되면 품질 축이 오염되고, 그 위에서 돌린 A/B는 믿을 수 없는 값이 된다.** 수정: `starterFiles` + 모든 `stages[].unlockPath` + `hiddenTestsPath`가 제공한 파일의 sha256을 모아두고, 워크스페이스 파일이 **바이트 단위로 같으면** 내용을 빼고 목록에만 "(제공된 입력, 변경 없음)"으로 남긴다. **경로로 넘겨짚지 않는 게 핵심**이다(`.claude` 제외를 되돌렸던 `report-xlsx` 교훈) — "우리가 준 그대로인 파일"만 빼므로 방법에 중립적이고, 참가자 작업물이 빠지려면 그게 우리 파일과 바이트가 같아야 하는데 그건 곧 "아무것도 안 고쳤다"는 뜻이라 잃는 정보가 없다. **회귀 확인(저장된 워크스페이스 80개 전부, `scripts/verify-judge-input-exclusion.mjs`)**: 제공한 적 없는 파일이 제외된 경우 0건이고, **예산(60,000자)을 넘겨 참가자 산출물이 잘리던 워크스페이스 30개가 예산 안으로 들어왔다** — 이 버그는 감점 사유를 만들어냈을 뿐 아니라 그동안 상당수 run에서 **산출물이 채점기에 아예 도달하지 못하게** 하고 있었다. 입력 파일을 주는 **모든 문제**의 채점 정확도가 같이 올라간다(`docs/evaluation.md`).

- **채점은 "어떻게 풀었는가"에 중립이어야 한다 — 실사용 run으로 방법 편향을 확정하고 수정(2026-08-11)**: "평가에 꼭 스킬을 써야 통과하거나 점수를 주는 부분이 있으면 빼라, 스킬은 의도한 방법 중 하나일 뿐이고 다른 방법에도 기회가 열려야 한다"는 요구에서 시작해, 실제로 `report-xlsx-staged`를 스킬 사용/미사용으로 두 번 돌린 결과로 확정했다. **문제 쪽은 깨끗했다** — 히든 테스트는 결과 `.xlsx` 파일만 보므로 어떤 도구가 만들었는지 알 방법 자체가 없고, 서로 독립된 세 경로(exceljs / openpyxl+LibreOffice / 순수 Python xlsxwriter)가 전부 20/20 통과하는 것까지 확인했다. **문제는 전부 채점자에게 무엇을 보여주느냐에 있었다**: 두 run 다 히든 테스트를 전부 통과했는데 LLM 점수만 크게 갈렸고(스킬 쪽이 6항목 중 4개에서 1점), 원인은 버그 3개였다 — ① `readFileSync(f,"utf8")`가 바이너리에 예외를 안 던져서 산출물 `.xlsx`가 깨진 텍스트로 통째로 실림, ② 경로순으로 60,000자를 채워서 스킬에 딸려온 OOXML 스키마(`sml.xsd` 246KB 등 `.xsd` 25개)가 예산을 독차지, ③ 채점자가 **테스트로 이미 확정된 사실을 소스만 보고 뒤집음**(20/20 통과한 run의 "캐시값" 항목에 1점). **여기서 내가 한 번 잘못 고쳤다**: ②를 보고 `.claude`를 제외했는데, 실제 run을 열어보니 **참가자 산출물이 거기 있었다**(`npx skills add`가 만든 `.claude/skills/xlsx/scripts/` 옆에 에이전트가 `build_report_step1~4.py`를 씀) — 산출물 위치는 방법마다 다르므로 **경로로 넘겨짚으면 안 된다**, 제외는 되돌렸다. 최종 수정은 바이너리 실제 차단(NUL 검사) + **작은 파일부터** 예산 채우기(손으로 쓴 산출물은 작고 딸려온 자료는 크다 — 어디에 뒀든 먼저 들어간다) + 파일 목록을 맨 앞에 명시 + `.xsd`류 확장자 제외 + 파일당 12,000자 상한 + **자동 테스트 결과를 채점자에게 알려주기**(통과했으면 다시 의심하지 말고 테스트가 못 재는 것만 보라). 재채점 결과 4.00 vs 3.25로 **근거 있는 실질 차이**가 됐다(스킬 쪽이 카테고리 목록을 `CATEGORY_ORDER`에 하드코딩 — 고정 csv를 쓰는 테스트는 못 잡는 진짜 결함). **여기서 얻은 규칙: rubric에 자동 테스트가 이미 결정론적으로 검증하는 항목을 넣지 마라** — 겹치면 신호는 안 늘고 오판만 는다. rubric은 테스트가 못 재는 것(재현성, 하드코딩 의존, 구조/가독성, 예외 처리)만 담는다(`docs/evaluation.md`).

- **시간을 점수에 다시 넣었다 — "적정 시간 초과분만큼 감점"(2026-08-11, 기존 결정 뒤집음)**: 그전까지 시간 가중치는 **0%**였고 근거는 "`maxDurationMs` 하드컷이 이미 시간 초과를 실격으로 배제하니 점수에서 또 깎으면 이중 벌점"이었다(2026-08-06). **실측이 그 전제를 무너뜨렸다** — 저장된 완료 run 33건의 소요 시간이 전부 **1~14분**인데 하드컷은 40~120분이라 **한 번도 작동한 적이 없다.** 이중 벌점이 아니라 애초에 벌점이 없었고 시간은 사실상 무제한이었다. 그래서 `Problem.targetDurationMs`(적정 시간)를 새로 두고, 적정 시간을 넘긴 순간부터 하드컷까지 **선형으로 최대 20점**을 종합 점수에서 뺀다(`runDisplay.ts`의 `computeTimePenalty`/`MAX_TIME_PENALTY`). 빨리 끝냈다고 가산점은 없다 — 있으면 품질을 희생해 서두르는 게 이득이 된다. 20점인 이유는 통과선이 80점이라 **완벽한 100점짜리가 하드컷 직전까지 끌면 정확히 통과선에 걸치게** 되는 지점이기 때문. 문제별 시간도 실측에 맞춰 다시 조였다(적정→하드컷): `report-xlsx-staged` 10→15분(사용자 지정), `csv-summarizer`/`inventory-digest`/`access-audit` 15→25분, `game-tactics` 30→50분. **소급 안전성 확인**: 저장된 완료 run 33건에 새 계산식을 돌려 **점수 변동 0건, 새 하드컷 초과 0건**. 사람이 관리할 수 없는 기준으로 깎으면 안 되므로 진행 막대에 적정 시간을 세로선으로 표시하고 넘으면 주황색으로 경고한다(`docs/evaluation.md`, `docs/problem-set.md`).

- **`inventory-digest-staged`를 7단계 · 3개 직무 릴레이로 전면 재설계(2026-08-11)**: 바로 아래 2단계 수정 직후, 실사용자가 **"곁다리가 하나뿐이라 컨텍스트를 안 나눠도 차이가 크게 안 난다"**고 지적해서 구조 자체를 다시 짰다. **서로 완전히 무관한 직무 세 개를 블록으로 끼운 7단계**다: 1~2 [물류팀] 재고 집계·카테고리별 집계+재고 부족(`inventory.csv` 0.7KB) → 3~4 [CS팀] 환불/교환 후기 추출·후기 통계(`reviews.json` **44KB**) → 5~6 [회계팀] 경비 규정 위반·부서별 집계(`expenses.json` **52KB**, 신규) → **7 [물류팀 복귀] 재주문 리포트**. 설계 포인트 셋: ① 직무가 블록이라 **정리 지점이 명확**하다(3단계로 넘어가면 물류 데이터가, 5단계로 넘어가면 후기 데이터가 죽는다), ② 정리 안 하면 7단계 즈음 **죽은 데이터 96KB(약 3만 토큰)**가 매 턴 캐시 재독으로 나간다(이전엔 44KB × 이후 2단계, 지금은 96KB × 이후 최대 4단계), ③ **7단계가 첫 직무로 돌아와서** 1·2단계에서 채팅으로만 받은 규칙(카테고리 코드, 카테고리별 재고 부족 기준)을 다시 쓰게 만든다 — 컨텍스트를 지운 사람은 그걸 **파일/CLAUDE.md로 외부화해뒀는가**에서 갈린다. **모든 단계의 판정은 기계적으로 확정**되고 오답 유도는 경계값으로만 준다(5단계 경비: 식대는 1인당 초과, 교통비는 20,000원 이하면 영수증 면제, 접대비는 총액 초과, 비품은 예외 — 정답 35건인데 흔한 오답은 84/54/25/62건). **검증 완료(27개 체크 전부 통과)**: 정석이 1→7단계 순서대로 전부 통과+히든 통과 / 오답 9종이 **정확히 의도한 단계에서만** 처음 실패 / 1·4단계 조기 완료 모두 히든이 차단. **게이트 완화 + Haiku 기준 예산 (같은 날 후속)**: 실사용에서 3단계 실패가 잦았는데 원인이 규칙 오해가 아니라 400건에서 한두 개 흘리는 **단순 누락**이었다 — 측정 축이 아닌 곳에서 결과가 갈리는 같은 종류의 문제라, id를 열거하는 단계(3·5)만 **누락/오검출 각각 2건까지 허용**하도록 완화했다(집계 숫자를 내는 단계는 그대로 정확 일치). 실측 확인: 2건 누락 통과 / 3건 누락 차단 / 규칙 오해 5종은 전부 차단. 예산은 **Haiku로 한 세션 완주 시 약 600,000 가중 토큰**이라는 실사용 실측에 맞춰 하드컷 **$0.7(=700,000 가중 토큰)** / 기준선 **500,000**으로 잡았다. ⚠️ **부작용 — 이 문제는 사실상 Haiku 전용이 된다**: Sonnet 5는 Haiku의 3배, Opus 5는 5배 단가라 같은 작업이 1,800,000 / 3,000,000 가중 토큰이 되어 하드컷을 넘긴다. 가중 토큰 설계의 의도된 성질이지만, 예산을 가장 싼 모델에 딱 맞추면 다른 모델 선택지가 사라진다(Sonnet까지 허용하려면 $2.0 이상 필요). **미검증**: 시간(적정 25분→45분)은 여전히 외삽이고, 재설계 후 A/B도 아직 안 돌렸다(`docs/problem-set.md`, `docs/guides/inventory-digest-staged.md`).
- **`inventory-digest-staged` 2단계 재설계 + 단계 수 고정 해제(2026-08-11)**: 실사용자가 두 가지를 지적했다. ① **"곁다리 요청에서 실패할 때도 있고 성공할 때도 있으면 의도가 엇나간다"** — 2단계를 "배송 관련 진짜 불만만 골라내기"라는 **판단 작업**으로 만든 건 스크립트로 못 접게 해서 원본이 반드시 컨텍스트에 올라가게 하려는 의도였는데, 판정이 흔들리니까 이 문제가 재는 게 컨텍스트 관리가 아니라 **"분류 운"**이 돼버렸다. **측정 축이 아닌 곳에서 결과가 흔들리면 그 문제는 목적을 잃는다** — 이게 문제 설계의 일반 원칙이다. 그래서 규칙을 한 문장의 확정적 판정으로 바꿨다: **별점 2점 이하 AND 본문에 `환불`/`교환` 포함**(정답 23건). 오답 유도는 판단이 아니라 경계로만 준다 — 별점은 낮은데 단어 없음 120건(별점만 보면 143건), 단어는 있는데 별점 높음 18건(단어만 보면 41건). 데이터는 재생성했고 산출물 파일명도 `delivery-complaints.json` → `refund-requests.json`으로 바꿨다. **대가는 의도적으로 받아들였다**: 이제 스크립트로 접을 수 있어서 원본이 컨텍스트에 안 올라갈 수도 있는데, **그건 실패가 아니라 좋은 결과다** — 큰 데이터를 대화에 안 올리고 처리하는 것 자체가 이 문제가 재려는 컨텍스트 관리의 정답 중 하나이고, 나쁜 결과("올려놓고 끝까지 들고 감")는 토큰 축에서 그대로 드러난다. 통합 검증 재실행: 정석 4단계+히든 통과 / `ratingOnly`·`wordOnly` 두 우회 모두 2단계에서 정확히 차단 / 조기 완료는 히든이 차단. ② **단계 수 4 고정 해제** — 처음 만든 문제들이 전부 4단계였던 게 우연인데 문서가 그걸 전제처럼 서술해서 새 문제도 4에 맞추게 되는 부작용이 있었다. 러너/대시보드는 원래 `stages.length` 기반이라 코드 변경 없이 N단계를 처리한다. `loadProblem`에 **2~10** 범위 검증만 추가했다(하한 2: 1단계짜리는 `stages`를 쓸 이유가 없다 / 상한 10: 현실적 상한 겸 오타 안전장치). **단계 수는 문제의 성격이 정한다 — 다른 문제가 4단계라는 이유로 단계를 채우지 마라**(`docs/problem-set.md`).

구현하면서 결정되는 세부사항은 해당 `docs/*.md`에 반영해 최신 상태로 유지한다.

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
